<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Epic" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>

    </style>

    <style name="Theme.Epic" parent="Base.Theme.Epic" />

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item> <!-- Background color -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/epic_logo_splashscreen</item>
        <item name="windowSplashScreenAnimationDuration">2000</item> <!-- Duration -->
        <item name="postSplashScreenTheme">@style/Base.Theme.Epic</item> <!-- Main theme -->
    </style>

</resources>